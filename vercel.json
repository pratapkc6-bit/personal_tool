{
  "framework": "nextjs",
  "installCommand": "npm install --legacy-peer-deps",
  "buildCommand": "npx prisma generate && if [ -n \"$DATABASE_URL\" ]; then npx prisma db push; else echo 'DATABASE_URL not set; skipping Prisma schema sync'; fi && next build",
  "env": {
    "NEXTAUTH_URL": "https://pratap-personal-secretary-ksuyanpks-projects.vercel.app",
    "APP_TIMEZONE": "Australia/Darwin"
  }
}
